# 底层原理：执行 `Future` 和任务

在本节中，我们将介绍 `Future` 和异步任务是如何调度的底层结构。如果你只对学习如何编写使用现有 `Future` 类型的高级代码感兴趣，而不对 `Future` 类型的工作细节感兴趣，你可以跳到 `async`/`await` 章节。然而，本章讨论的几个主题对于理解 `async`/`await` 代码的工作原理、理解 `async`/`await` 代码的运行时和性能属性以及构建新的异步原语非常有用。如果你决定现在跳过本节，你可能希望将其加入书签以便将来重温。

现在，话不多说，让我们来谈谈 `Future` trait。
